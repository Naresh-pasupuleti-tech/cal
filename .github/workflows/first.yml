name: Cal CI
 
on: push
 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
 
      - run: echo "Hello From CI"
 
      - run: ls -R
 
      - run: bash main.sh
        working-directory: shell_script/cal
