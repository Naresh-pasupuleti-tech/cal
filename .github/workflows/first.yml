name: Cal CI
 
on: push
 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
 
      - run: echo "Hello From CI"
 
      - run: ls -R
 
      - run: |
          cd shell_script/cal
          bash main.sh
 
