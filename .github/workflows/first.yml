
name: Calculator Workflow

on:
  push:
    branches:
      - main
    runs-on: ubuntu-latest  workflow_dispatch:  # Allows manual trigger
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Make scripts executable
        run: chmod +x *.sh

      - name: Run main.sh
        run: ./main.sh

jobs:
  run-calculator:

